# 2026-08-14

## 今日のテーマ

- Webアプリケーションのレイテンシ
- RTT、TTFB、総レスポンス時間の違い
- 平均、p95、p99の読み方
- 指標が悪化した場合の調査手順

## 学んだこと

### レイテンシ

- Webアプリケーションのレイテンシは、リクエストを開始してからレスポンスを受け取るまでの遅延
- ネットワークの往復だけでなく、nginx、Node.js、DB、各種の待ち時間、レスポンス転送時間などを含む
- レイテンシは物理的な距離だけで決まらず、SQL、アプリケーション処理、キャッシュ、待ち行列、レスポンスサイズなども影響する
- 高速化しても、レスポンスの正しさを壊す変更は採用しない

### RTT、TTFB、総レスポンス時間

- RTT（Round Trip Time）は、通信相手との往復にかかる時間
- TTFB（Time To First Byte）は、リクエスト開始からレスポンスの最初の1バイトがクライアントへ届くまでの時間
- 総レスポンス時間は、レスポンス全体を受信し終わるまでの時間
- TTFBが長い場合は、最初の1バイトが返るまでのネットワーク、待機、Node.js、DBなどを疑う
- TTFBは短いが総レスポンス時間が長い場合は、レスポンスサイズ、転送速度、ストリーミング処理などを疑う

```text
クライアント
  │
  │ ① HTTPリクエストを送る
  ▼
ネットワーク
  │
  ▼
nginx
  │
  │ ② Node.jsへ転送する
  ▼
Node.js
  │
  │ ③ 必要に応じてSQLを発行する
  ▼
MySQL
  │
  │ ④ SQLの結果を返す
  ▼
Node.js
  │
  │ ⑤ HTTPレスポンスを作り始める
  ▼
nginx
  │
  │ ⑥ レスポンスをクライアントへ送る
  ▼
ネットワーク
  │
  │ ⑦ 最初の1バイトがクライアントへ到着
  │     ↑ ここまでがTTFB
  │
  │ ⑧ 残りのレスポンス本文がクライアントへ到着
  │     ↑ ここまでが総レスポンス時間
  ▼
クライアント
  │
  │ ⑨ JSON解析、JavaScript実行、HTML更新、画面描画
  ▼
表示完了
```

- TTFBの終点は、HTTPクライアントがレスポンスの最初の1バイトを読み取れるようになった時点
- レスポンス受信後のJSON解析、JavaScript実行、HTML更新、画面描画は通常TTFBに含まれない
- DNS、TCP接続、TLS接続などを計測に含めるかはツールや計測開始点によって異なる

### 順番待ち

- DBコネクションプールなどの資源がすべて使用中の場合、空くまで実質的なキューで待つ
- 設定された待ち時間を超えると、タイムアウトやエラーになる場合がある
- DB関連の時間も、コネクション取得待ちとSQL実行時間に分けて考える

### 平均、p95、p99

- 平均は、すべてのレイテンシの合計をリクエスト数で割ったもの
- p95は、約95%のリクエストがその時間以内に完了する境界
- p99は、約99%のリクエストがその時間以内に完了する境界
- p99は「一番重い種類のリクエスト」ではなく、観測したリクエストを速い順に並べたときの遅い側1%付近の境界
- 平均とp99は原因を特定する指標ではなく、全体や遅い側の変化を検知する指標
- p99は少数の遅いデータを見るため、複数回計測して再現性を確認する
- 平均やスコアにも揺らぎがあるため、可能なら同じ条件で複数回計測する

## 自分の言葉で説明

### レイテンシとは

クライアントがリクエストを飛ばしてから、サーバーを経由してレスポンスが返ってくるまでの時間。ネットワークだけでなく、プロキシサーバー、アプリケーション、DBなどの処理や待ち時間も含まれる。

### 平均とp99の違い

平均では全体的な性能の変化を見る。p99では、遅い側1%付近のリクエストがどの程度の時間で完了したかを見る。どちらも変化を検知した後は、ログから実際のリクエストを抽出して原因を調査する。

### p99が悪化した場合の調査手順

1. 同じ条件で複数回ベンチを実行し、悪化が再現するか確認する
2. アクセスログから時間がかかった実リクエストを抽出する
3. エンドポイントや発生条件ごとに分類する
4. Node.js、SQL、DBコネクション待ちなどへ処理時間を分解する
5. 原因候補を修正して再計測する

## 理解度チェック

- [x] RTT、TTFB、総レスポンス時間を区別できる
- [x] TTFBの前後で、疑う箇所を分けられる
- [x] p95とp99の意味を説明できる
- [x] 平均とp99の変化を別々に評価できる
- [x] p99悪化後の調査手順を並べられる
- [x] 正しさを壊す高速化を採用しないと判断できる

## 理解度チェックでの補足

- 平均が改善しても、特定のボトルネックが解消したとは限らない
- 平均の改善から断定できるのは、全体傾向としてレイテンシが改善したこと
- p99が悪化した場合は、遅い側で問題が起きた可能性を考え、実際の遅いリクエストを調査する
- `p99全体 = nginxのp99 + Node.jsのp99 + DBのp99`とはならない。各p99が同じリクエストを示すとは限らないため、実リクエスト単位で追跡する

## 分からないこと・今後確認すること

- 実際のアクセスログから平均、p95、p99を計算する方法
- nginxでリクエスト時間を記録する方法
- Node.js、SQL、DBコネクション待ちを実際に切り分ける方法

## 次にやること

- スループットと並行数・平均レイテンシの関係を復習する
- ボトルネックとISUCONでのスループットの見方を学ぶ

## 追記：スループット

### 学んだこと

- スループットは、一定時間内に完了できた仕事の量
- Webサービスでは、主に単位時間あたりに正常完了したリクエスト数として扱う
- 基本式は `スループット = 正常完了したリクエスト数 ÷ 経過時間`
- 単位には `req/s` や RPS（Requests Per Second）が使われる
- 受け付けただけで処理待ちのリクエストや、エラーになったリクエストは、今回扱った正常リクエストのスループットには含めない
- 観測したスループットと最大スループットは異なる。最大スループットを知るには、負荷を増やしてスループットが伸びなくなる限界を調べる必要がある

### スループットとレイテンシの違い

- スループットは、システム全体が単位時間あたりに完了できたリクエスト数
- レイテンシは、個々のリクエストを送ってからレスポンスが返るまでの時間
- 例として、10秒間に500件が正常完了した場合のスループットは `500件 ÷ 10秒 = 50 req/s`

### 並行数との関係

- 並行数は、同じ時点でシステム内にいる処理中または待機中のリクエスト数
- リクエストが安定して流れている場合、次の関係を目安にできる

```text
並行数 = スループット × 平均レイテンシ
スループット = 並行数 ÷ 平均レイテンシ
平均レイテンシ = 並行数 ÷ スループット
```

- この関係は、距離・速さ・時間の公式と同じ形をしている
- 道路に例えると、道路上の車の台数が並行数、1秒間に道路を通り抜ける車の台数がスループット、1台が道路上に滞在する時間が平均レイテンシに対応する
- 平均レイテンシが0.25秒なら、1つの処理枠は1秒間に平均4件を完了できる。20件を並行処理できる場合は、理想的には `4件/秒 × 20 = 80 req/s` となる
- 並行数を増やしても、CPU、DB接続、ロックなどの上限に達すると、スループットは伸びずレイテンシだけが悪化する場合がある

### 自分の言葉で説明

スループットは一定時間内で正常に処理した件数、レイテンシはリクエストを飛ばしてからレスポンスが返ってくるまでの時間。

より厳密には、スループットは単位時間あたりに正常完了したリクエスト数を指す。

### 理解度チェック

- [x] 正常完了件数と計測時間からスループットを計算できる
- [x] スループットとレイテンシを区別できる
- [x] 観測されたスループットだけでは最大スループットを断定できないと判断できる
- [ ] `スループット = 並行数 ÷ 平均レイテンシ` となる理由を自分の言葉で説明できる
- [ ] 並行数を増やしてもスループットが必ず比例して増えるわけではない理由を説明できる
- [ ] ボトルネックがスループットとレイテンシへ与える影響を説明できる
- [ ] ISUCONのベンチマーク結果からスループットを読み取れる

### 理解度チェックでの補足

- 20秒間に1,600件が正常完了した例では、`1,600件 ÷ 20秒 = 80 req/s` と正しく計算できた
- `80ms` がスループットではなくレイテンシであると正しく区別できた
- 観測値だけでは最大スループットを断定できないと正しく判断できた
- 最大スループットを確認するには、負荷や並行数を増やし、CPU、DB、接続数などが限界に達してスループットが伸びなくなる点を調べる

### 次回確認すること

- `スループット = 並行数 ÷ 平均レイテンシ` の直感的な意味を定着させる
- 距離・速さ・時間の公式、および道路を流れる車の例との対応を自分の言葉で説明する
- 並行数を増やしたとき、どこまでスループットが伸び、どこからレイテンシだけが悪化するのか
- CPU、DB、コネクションプール、ロックなどのボトルネック
- ISUCONでスループットをどう観測し、改善前後をどう比較するか

## 学習時間

- 2時間19分
